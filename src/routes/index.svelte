<!-- index.svelte -->

<!-- <script context="module">
	export const load = async ({ fetch }) => {
		const posts = await fetch('../posts/*.md');
		const allPosts = await posts.json();

		return {
			props: {
				posts: allPosts
			}
		};
	};
</script>

<script>
	export let posts;
</script>

<ul>
	{#each posts as post}
	
		<li>
			<a href={post.path}>
				{post.meta.title}
			</a>
			<i>(Published {post.meta.date})</i>
		</li>
	{/each}
</ul> -->

here

